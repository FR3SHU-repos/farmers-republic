[build.environment]
  # These values are public API origins intentionally embedded by Next.js.
  # Keep secret scanning enabled for every credential-bearing variable.
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_API_BASE_URL,NEXT_PUBLIC_CATALOGUE_API_BASE_URL"
